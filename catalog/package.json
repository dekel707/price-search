{
  "name": "fujicom-dealer-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:fallback": "node scripts/generate-fallback.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
